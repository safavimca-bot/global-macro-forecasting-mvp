export default function Loading() {
  return (
    <div className="rounded-md border border-white/10 bg-white/[0.04] p-6 shadow-panel">
      <p className="text-sm font-semibold text-white">Loading macro dashboard...</p>
      <p className="mt-2 text-sm text-slate-400">Preparing data, scores, charts, and outlook text.</p>
    </div>
  );
}
